# Orivo homepage layout + logo fix

Upload `index.html` to the repo root and let it replace the current homepage.

This file:
- widens the hero layout so the left side no longer feels empty on large screens
- keeps the phone conversation mockup
- changes the nav/footer logo to use `/assets/logo.png`
- falls back to `/logo.png` if the asset copy is not available yet
- keeps the premium motion script enabled

Do not edit or upload CNAME.

After upload, test:
https://orivohealth.com/?v=layout-logo-fix
